import React from "react";
import { CorrelationWrapper } from "@/features/proteinpaint/CorrelationWrapper";

const CorrelationApp: React.FC = () => {
  return <CorrelationWrapper />;
};

export default CorrelationApp;
